# This is an example nfpm configuration file.
# Make sure to check the documentation at https://nfpm.goreleaser.com
#
# The lines below are called `modelines`. See `:help modeline`
# Feel free to remove those if you don't want/need to use them.
# yaml-language-server: $schema=https://raw.githubusercontent.com/Bubble-droid/nfpm/refs/heads/feat/arch/www/static/schema.json
# vim: set ts=2 sw=2 tw=0 fo=cnqoj

name: 'gui-for-singbox-xdg'
arch: ${ARCH}
platform: linux
version: ${VERSION}
section: default
priority: optional

maintainer: ${PACKAGER}

vendor: Bubble-droid
homepage: 'https://github.com/Bubble-droid/GUI.for.SingBox'
license: GPL-3.0-or-later
description: |
  ${DESCRIPTION}

provides:
  - gui.for.singbox
  - gui-for-singbox

conflicts:
  - gui.for.singbox
  - gui-for-singbox
  - gui-for-singbox-bin
  - gui-for-singbox-xdg-full

contents:
  - src: ./build/bin/gui-for-singbox
    dst: /usr/bin/gui-for-singbox
    file_info:
      mode: 0755
  - src: ./package/linux/gui-for-singbox.desktop
    dst: /usr/share/applications/gui-for-singbox.desktop
    file_info:
      mode: 0644
  - src: ./package/linux/gui-for-singbox.rules
    dst: /usr/share/polkit-1/rules.d/50-gui-for-singbox.rules
    file_info:
      mode: 0644
  - src: ./build/appicon.png
    dst: /usr/share/icons/hicolor/512x512/apps/gui-for-singbox.png
    file_info:
      mode: 0644
  - src: ./LICENSE
    dst: /usr/share/licenses/gui-for-singbox/LICENSE
    file_info:
      mode: 0644

scripts:
  postinstall: ./package/linux/postinstall.sh
  postremove: ./package/linux/postremove.sh

overrides:
  archlinux:
    depends:
      - gtk3
      - webkit2gtk-4.1

  deb:
    depends:
      - libgtk-3-0
      - libwebkit2gtk-4.1-0
    suggests:
      - pkexec
      - libcap2-bin
      - libkf6config-bin
      - libglib2.0-bin
      - network-manager

  rpm:
    depends:
      - gtk3
      - webkit2gtk4.1
    suggests:
      - polkit
      - libcap
      - kf6-kconfig
      - glib2
      - NetworkManager

deb:
  compression: zstd
  fields:
    Bugs: 'https://github.com/Bubble-droid/GUI.for.SingBox/issues'
    X-Upstream-Author: 'Bubble-droid'

archlinux:
  packager: ${PACKAGER}
  optdepends:
    - 'polkit: Interactive authorization for privileged operations'
    - 'libcap: Grant network capabilities to core binaries'
    - 'kconfig: Configure KDE system proxy'
    - 'glib2: Configure GNOME system proxy'
    - 'networkmanager: Automatically configure system DNS'
  scripts:
    postupgrade: ./package/linux/postinstall.sh

rpm:
  compression: zstd
